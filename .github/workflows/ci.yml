name: CI

on:
  pull_request:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-test:
    if: github.repository == 'svnscha/mcp-windbg'
    uses: ./.github/workflows/build-and-test.yml
